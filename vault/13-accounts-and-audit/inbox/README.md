# inbox

Drop new CA files here, then file each one with:

    python ops/intake.py vault/13-accounts-and-audit/inbox/<file> --kind <kind> --fy <yyyy-yy> --date <YYYY-MM-DD>

This folder must be empty at the end of every monthly close (SOP-08).
