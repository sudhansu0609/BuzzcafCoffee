# Buzzcaf Ops runtime
fastapi>=0.115,<1.0
uvicorn>=0.30,<1.0
jinja2>=3.1
python-multipart>=0.0.9
pydantic>=2.7,<3.0
markdown>=3.5
# inbox sorter + assistant
pypdf>=4.0
openpyxl>=3.1
anthropic>=0.60      # optional: only used when ANTHROPIC_API_KEY is set
# OCR for scanned PDFs and photos (optional but recommended; pure pip, no external binary)
pypdfium2>=4.30
rapidocr-onnxruntime>=1.3
pillow>=10.0
# tests
pytest>=8.0
httpx>=0.27
